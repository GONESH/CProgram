// Use the library function to calculate the area of a square with side a.
#include <stdio.h>
#include <math.h> // library function

int main()
{
    double a=2.2; // Declare a variable to store the side length of the square.
    printf("The area of a Square is %lf",pow(a,2)); // lf -long float
    return 0;
}